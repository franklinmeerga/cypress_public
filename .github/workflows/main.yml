name: Cypress test
on: [push]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Cypress.io
        uses: cypress-io/github-action@v2.9.7
        with:
          start: npm run test:chrome
   
