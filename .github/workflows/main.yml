name: Cypress test
on: [push]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Cypress.io
        uses: cypress-io/github-action@v9.5.1
        with:
          start: npx start
   
